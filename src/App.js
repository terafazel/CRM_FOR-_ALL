import React from "react";

function App() {
  return (
    <div style={{ padding: "20px", fontFamily: "sans-serif" }}>
      <h1>CRM App</h1>
      <p>
        Frontend is running! 🎉
      </p>
      <p>
        Backend should be running at <code>http://localhost:8000</code>.
      </p>
      <p>
        Next step: we can build out pages for Accounts, Contacts, Activities, and
        Follow-ups.
      </p>
    </div>
  );
}

export default App;
